<Window x:Class="PrimerNeki.Editor"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:PrimerNeki"
        mc:Ignorable="d"
        Title="Editor" Height="450" Width="800" DataContextChanged="PromenaDT">
	<UniformGrid Columns="2">
		<Label FontSize="24">Naziv: </Label>
		<TextBox Text="{Binding Naziv}" FontSize="24"/>
		<Label FontSize="24">Ulazna:</Label>
		<TextBox Text="{Binding UlaznaCena}" FontSize="24"/>
		<Label FontSize="24">Marza:</Label>
		<TextBox Text="{Binding MarzaProc}" FontSize="24"/>
		<Button FontSize="24">Otkazi</Button>
		<Button FontSize="24" Click="OK">OK</Button>
	</UniformGrid>
</Window>
